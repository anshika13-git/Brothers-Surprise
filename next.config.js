/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // For GitHub Pages - update this to your repository name
  // basePath: '/Brothers-Surprise',
  // assetPrefix: '/Brothers-Surprise',
}

module.exports = nextConfig

